import React from "react";
import Card from '../components/card'
import ButtonOptions from '../components/buttonoptions'


export default class CardButtonCombo extends React.Component {
     return (
       <div>

       // wanting the information shown in the Card and in the Button Options to match by default, and act as a template that could theoretically be fed with data from an API/database/etc
<Card></Card>
<ButtonOptions></ButtonOptions>
</div>
)
};
